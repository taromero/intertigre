Functional test with Geb and Selenium.
	To run the tests with the chrome driver, download the driver from http://code.google.com/p/chromedriver/downloads/list.
	Extract it to the root of the project and run 'grails -Dgeb.env=chrome test-app functional:'
